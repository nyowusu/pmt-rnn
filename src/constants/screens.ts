import {name as appName} from '../../app.json';

export const ScreenNames = {
  HomeScreen: `${appName}.Home`,
  PaymentScreen: `${appName}.PaymentScreen`,
  BankAccountScreen: `${appName}.BankAccountScreen`,
  MobileMoneyScreen: `${appName}.MobileMoneyScreen`,
  PayPalScreen: `${appName}.PayPalScreen`,
  CardPaymentScreen: `${appName}.CardPaymentScreen`,
};
